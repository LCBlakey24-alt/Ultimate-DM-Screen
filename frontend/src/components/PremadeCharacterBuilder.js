import React from 'react';
import BasicCharacterBuilder from './BasicCharacterBuilder';

// MVP: reuses basic builder. Emergent can replace this with curated templates + AI matching tab.
export default function PremadeCharacterBuilder() {
  return <BasicCharacterBuilder />;
}
